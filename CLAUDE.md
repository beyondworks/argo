# Argo — saas 프로젝트 (구 코드네임 crewbase)

> 생성일 2026-07-10. 이 파일은 루트 `lean-projects/CLAUDE.md`와 글로벌 `~/.claude/CLAUDE.md`를
> 상속한다. 아래 도메인 에이전트 세트가 기본 라우팅이며, 프로젝트 고유 규칙이 그 위에 우선한다.

## North Star (사용자 결과 한 문장 — 고정값)

> 일반 사용자가 가입해 프롬프트 한 줄로 AI 직원 회사를 만들고, 폴더 단위 기억으로 일 시키는 SaaS

- 작업 내내 이 문장을 최상단 고정값으로 둔다.
- **파일 3개/단계 1개마다** 자문: "방금 만든 게 North Star에 직접 기여하나? 곁가지로 샜나?"
- 표류 감지 시 한 걸음도 더 가지 말고 정지 → 재정렬.

## Metric Lock (성공 지표)

- (측정 가능한 성공 기준을 여기에. 미정 시 기본 규칙: "요청 변경 구현 + 무관 영역 미파손 + lint/test 통과")

## 프로젝트 고유 규칙 (도메인 기본을 덮어씀)

- 스택: Next.js(App Router, JS) + Claude Agent SDK 코어(`src/*.mjs`) — P1에서 Supabase 인증·DB 합류
- 배포처: 로컬 dev(현 단계) → Fly.io/Railway 워커 (P1)
- 외부 통합: Claude Agent SDK(BYOK — API 키/구독 OAuth 환경변수), MCP(후속)
- 주의사항: 워크스페이스 루트 env `ARGO_ROOT`(구 `CREWBASE_ROOT` 병행 수용). `workspaces/`는 gitignore — 사용자 데이터 커밋 금지
- 네이밍: **Argo** = 아르고호(전문 영웅들이 한 배로 황금양털 항해). 디자인 모티프 = 밤바다 네이비 + 황금양털 골드

## 검증

- 완료 선언 전: North Star ↔ 완성물 역대조. 빌드 통과 ≠ 목적 달성.
- 검수는 구현과 다른 컨텍스트의 에이전트가 (자기 승인 금지).

---

## 변경 안전 절차 — 오류·혼재·버그 0 (2026-07-15 유건 지시, 절대 규칙)

> 사소한 수정·디자인 변경·기능 추가 **모두** 이 절차를 따른다. 셋은 원인이 달라 방어도 다르다 — 변경 크기로 강도만 조절한다.

### 3대 방어선 (실패 유형별)

- **혼재 방지 — 올바른 베이스 + 격리 작업공간.** 작업 시작 전 step 0: "지금 이 코드가 제품 정본인가?"를 확인한다(`git merge-base`·`git log main..HEAD`로 베이스 최신성 점검). 아니면 **올바른 베이스에서 새 브랜치**를 판다(stale 브랜치 위 작업·검수는 있지도 않은 문제를 만든다 — 실측: landing이 제품 보안작업 누락된 stale이었음). **한 변경 = 한 워크트리.** 테스트는 **임시 데이터 루트(임시 `ARGO_ROOT`) + 별도 포트**로 — 실데이터·실행 중 서버(:3001 등) 미접촉. 실험이 실제 상태를 오염시키는 게 혼재의 본질.
- **오류 방지 — 읽고 나서 쓰기 + 최소 변경.** 추측한 줄번호·동작으로 고치지 않는다. 실제 파일을 읽고, **이미 도는 기존 패턴을 재사용**한다(예: 새 화면 로그아웃 = 회사 사이드바의 검증된 `/auth/signout` 폼 재사용). 바꾸는 표면을 최소화. `node --check`(.mjs)·`next build`로 문법·컴파일 확인 — 단 이는 "안 깨짐"이지 "동작"이 아니다.
- **버그 방지 — 수준에 맞는 실제 검증 + 분리 검수.** 빌드 통과 ≠ 동작. UI는 반드시 **띄워서 눈으로**(상태별·라이트/다크·모바일폭). 검수는 만든 컨텍스트가 아닌 **다른 컨텍스트**가 "목적 달성 + 회귀 없음"을 역검증(자기 승인 금지). 같은 계열 갭은 한 번에 안 닫히니 **수정 후 재검증을 반복**한다(실측: 경계 게이트 갭이 3라운드 만에 닫힘).

### 변경 크기별 체크리스트

- **사소(오타·한 줄·설정)**: 정확한 위치 읽기 → 수정 → 문법/빌드. 워크트리·무거운 계획 생략(과설계가 오히려 실수 유발).
- **디자인(UI)**: 수정 → 격리 dev 서버 → **브라우저 시각 확인(상태별)** → 스크린샷 증거. 빌드만으로 완료 선언 금지. 브라우저는 Aside 사용(글로벌 규칙).
- **기능**: 올바른 베이스 → 최소 출하버전(MVP) → 기존 패턴 재사용 → 실제 흐름 실행 → 분리 검수 → 검증 → 논리단위 커밋. 큰 리팩터 겸사겸사·여러 관심사 한 커밋 금지.

### 검증 범위 = 문맥 행렬 (2026-08-19 — 하루에 자책골 6건 내고 세운 규칙)

실패는 늘 같은 모양이었다: **바꾼 코드가 도는 문맥은 여럿인데, 떠올린 한 문맥에서만 확인**했다.
데크는 실행 안 하고 CSS만, 포털은 쿠키 있는 문맥에서만, commandExists는 맥에서만.

**1. 문맥을 먼저 나열하고 그 다음에 확인한다.** 이 레포의 축은 고정이다 —
`OS(맥·윈·리눅스) × 셸(브라우저 :3001·Tauri 웹뷰·헤드리스 사이드카) × 인증(쿠키 세션·기기 세션·
게스트·무인증) × 러너(SDK·CLI) × 플랜(free·trial·pro) × 산출물(main·앱 번들·셀프호스트 타르볼·
발행본)`. 바꾼 코드가 걸치는 축만 골라 각각 확인한다. **확인 못 한 축은 "미검증"이 아니라 "미완"**이다 —
완료 보고에 넣지 않는다.

**2. 삭제·제외는 대상 집합을 전부 나열한 뒤 하나씩 의도를 확인한다.** 와일드카드로 지우면 의도 밖이
반드시 섞인다(실사고: 내부 문서 제거가 사용자향 `docs/selfhost.md`까지 지움). `rm -r`·glob 제외·
gitignore·번들 스트립 전부 해당.

**3. "고쳤다"의 기준은 머지가 아니라 사용자에게 도달한 산출물이다.** main 머지 ≠ 반영. 상주(:3001)·
설치 앱·발행본은 각각 별개 산출물이고, 어디까지 갔는지 매번 명시한다(실사고: 게이트는 main에만 있고
발행본 v0.1.41은 게이트 없이 나감).

**4. 재촉받을수록 범위를 넓힌다.** 내 실패 모드는 정반대다 — 압박이 오면 확인을 줄이고 한 축만 본다.
그렇게 아낀 시간의 몇 배가 회귀로 돌아온다. 급할수록 "이게 도는 다른 문맥이 뭐지"를 한 번 더 묻는다.

**5. 심각도를 부풀리지 않는다.** 근거 없이 "턴 전체가 죽는다"고 하면 불필요한 핫픽스·공포를 만든다.
영향 범위도 사실로만 말한다(실사고: 깨진 MCP가 codex 턴을 죽인다고 단정했으나 실측은 진행됨).

### 보안·데이터 판정 겉핥기 금지 (2026-08-19 유건 지시 — 위반 금지)

> "겉핡기 식으로 대충 쓱 훑어보고 자격증명은 안 나갔다느니 보안이 좋다느니 지껄이는거같은데,
> 일 그따위로 하지마. 너한테는 그냥 작업일지 몰라도 나한테는 되게 중요한 일이거든?"

사고 조사 중 **안심시키는 문장을 먼저 쓰지 않는다.** 부재(유출 없음)는 증명 불가 —
부분 스캔으로 전체를 단정하면 그게 환각이다. 말할 수 있는 건 "검사한 표면에서 발견 안 됨"뿐.

- **검사 표면을 반드시 열거하고, 안 본 표면도 같이 적는다.** 이 레포에서 실제로 갈라지는 표면:
  추적본 grep(HEAD만·히스토리 아님) / `git log --all -S`(패턴별) / `public` 스키마 테이블 /
  **Storage 버킷 정책(고객 파일이 사는 곳)** / 엣지 펑션 / 예비 프로젝트(qbwr) / 설치 번들 /
  공개 레포·README 링크 / 랜딩 배포물.
- **패턴 하나로 "시크릿 없음" 선언 금지** — JWT(eyJ)·`postgresql://`·`sk-`·`re_`·webhook secret·
  private key를 각각 본다.
- **중요도 순서**: 고객 데이터가 실제 사는 곳 → 자격증명 → 공개 노출면. 눈에 띈 순서가 아니다.
- 실사고 2026-08-19: 설치 번들과 공개 레포 양쪽에 내부 문서가 실려 나갔고, README가
  `docs/security-encryption-roadmap.md`(미암호화 항목·격리 한계)를 링크로 걸고 있었다.

### 못 한 검증은 정직하게
자격증명 필요 등으로 라이브 검증 못 한 부분은 **"추론(코드 동일성) — 라이브 아님"**으로 명시한다. "완료/무결/검증 완료" 도장 금지(글로벌 communication 규칙).

### 마감 위생
커밋 전 `git diff --cached`에서 시크릿 평문(key/token/DATABASE_URL) grep. `git add .`/`-A` 금지 — 파일 명시 add. **논리 단위 커밋**(보안/기능/정리 분리, 커밋 간 빌드 유지). 파괴적 행위(삭제·force push·병합)는 **승인 후**.

---

## 도메인: saas (SaaS)

> 웹/앱 제품 개발, 풀스택, 배포. 사용자 결과 = **동작하는 제품 기능**.

### 표준 파이프라인

탐색(explore) → 계획(plan) → 구현 → 코드/보안 리뷰 → 동작 검증 → 배포

### 추천 에이전트 세트 (1단 기본 라우팅)

| 단계 | 우선 사용 | 비고 |
|---|---|---|
| 설계 | `system-architect`, `backend-architect`, `api-designer` | 구조 먼저 |
| 프론트 | `frontend-architect`, `react-specialist`, `nextjs-developer` | UI 구현 |
| 풀스택 구현 | `fullstack-developer`, `typescript-pro` | 경계 통합 |
| 배포 | `deployment-engineer`, `devops-engineer` | Vercel/systemd |
| 검수(분리) | `code-reviewer` + `security-engineer` + `test-automator` | 자기 승인 금지 |

### 추천 스킬 (작업 종류별)

- 제품 골격: `/saas-platform-builder`, `vercel:bootstrap`, `vercel:nextjs`
- 프론트 디자인: `/frontend-design`, `/ui-ux-pro-max`, `shadcn-ui` MCP
- 규율: `superpowers:test-driven-development`, `superpowers:systematic-debugging`
- 리뷰/배포: `/code-review`, `/verify`, `vercel:deploy`, `vercel:env`

### 도메인 검증 기준 (절대)

- **빌드 성공 ≠ 기능 동작.** UI 변경은 dev 서버 + 브라우저(playwright/ui-inspector) 시각 확인 후 완료.
- 배포 직후 엔드포인트 `curl` 응답 검증.
- 비동기 상태 변경은 렌더링 타이밍까지 확인.
- 시크릿 평문 금지(글로벌 보안 규칙). `.env`는 이름·위치만 기록.

## 다국어 상시 규칙 (2026-07-10 유건 지시 — 위반 금지)

- **모든 UI 문자열은 `app/i18n.jsx`의 사전(t)을 통해서만** 넣는다. 새 기능·페이지 추가 시 지시 없어도 ko/en 두 언어 모두 등록.
- 한국어 모드 = 고유명사(Argo, Claude 등) 제외 전부 한글. 마이크로라벨도 한글.
- 월 지출 한도는 한국어 모드에서 원화(₩, `fmtMoney` 고정 환산율) 표기.
- 언어 전환 단축키 `cmd+/` (i18n Provider가 처리). 언어 상태는 localStorage `argo-lang`.

## 삭제류 액션 규칙

- 크루 해고·회사 보관 등 파괴적 액션은 `ui.jsx`의 `DangerModal`(깃헙식 — 이름 입력 + 확인 문구) 사용. window.confirm/prompt 금지.
