# QA 피드백 백로그 — 2026-07-27

> 이 문서는 **QA 세션이 개발 세션에 넘기는 인수인계서**다. Supabase `public.feedback` 전량(41건)을
> `origin/main` 코드·릴리스 이력과 대조해 우선순위·근본원인·상태를 정리했다.
>
> - **데이터 출처**: Supabase 프로젝트 `argo`(ref `nvqorpdorlbkhpcvwoty`, ap-northeast-2) `public.feedback`,
>   2026-07-27 조회. 41행 전량(테스트 insert 2건 포함).
> - **대조 기준**: `origin/main` `a49c737` · Latest 릴리스 **v0.1.30**(2026-07-26T18:00:18Z).
>   **main이 v0.1.30보다 PR 4개(#118·#119·#120·#121) 앞서 있다 — 미발행 구간이다.**
> - **개인정보**: 이 레포는 **public**이다. 제보자 이메일은 마스킹했고 원문은 여기 적지 않는다.
>   회신이 필요하면 아래 `id` 앞 8자로 Supabase에서 조회한다.
>   `select email, message from public.feedback where id::text like '<id8>%';`

## 판정 표기

| 표기 | 뜻 |
|---|---|
| **해소(발행)** | 근본 수정이 머지·발행됨. 제보자 재확인만 남음 |
| **해소(미발행)** | 수정이 `main`에 있으나 릴리스 전 |
| **미해소** | 코드에서 원인 확인, 수정 없음 |
| **재현 필요** | 코드만으로 단정 불가. QA가 실기기 재현해야 함 |
| **기능 요청** | 결함 아님 |

각 항목의 "근거"는 이 문서를 쓰며 **직접 실행한 grep/`git show` 결과**다. 근거 없는 항목은 "미확인"으로 적었다.

---

## 요약

- 유효 제보 **39건**(테스트 insert 2건 제외). 제보자 **17명**.
- 07-25~07-27 사흘에 **32건**이 몰렸다 — 사용자 급증(73명) 구간과 일치.
- **최대 클러스터는 "홈 밖 경로 접근 차단 + 작업 폴더를 바꿀 수 없음" 11건**(제보자 7명). 단일 원인이다.
- v0.1.30(#113)이 이 클러스터를 **안내 측면에서만** 건드렸다 — 접근을 허용하지 않는다. 신고는 계속 들어온다.
- Gemini OAuth 미연결(2건)의 **근본원인이 제보자에 의해 규명**됐다 — 우리 버그가 아니라 Google의 벤더 측 폐기.

### 우선순위

| 순위 | 항목 | 건수 | 상태 |
|---|---|---|---|
| **P0-1** | 홈 밖 경로 차단 + 작업/볼트 루트 변경 불가 | 11 | 미해소 |
| **P0-2** | Gemini OAuth 경로가 벤더 측에서 폐기(Antigravity 전환) | 2 | 미해소 |
| **P1-1** | 크루 이름이 전부 "서윤"으로 생성 | 1 | 미해소(원인 확정) |
| **P1-2** | 장시간 작업 300초 강제 종료가 ENOENT로 위장 | 1 | 재현 필요 |
| **P1-3** | 회의실 대화 유실 | 1 | 재현 필요 |
| **P2** | 자동 모델 재선택 · shift+엔터 | 2 | 재현 필요 |
| — | 기능 요청 | 13 | — |
| — | 이미 해소(재확인만) | 8 | 해소 |

---

## P0-1 — 홈 밖 경로 차단 + 작업/볼트 루트 변경 불가 (11건, 제보자 7명)

**가장 많이 들어오는 신고다. 원인은 하나다.**

### 근본 원인 (코드 확인)

1. **codex 러너의 쓰기 허용 루트가 홈으로 하드코딩돼 있다.**
   - `src/runners.mjs:459` — `sandbox_workspace_write.writable_roots=[${JSON.stringify(homedir())}]`
   - `src/runners.mjs:518` — `writable_roots = [${JSON.stringify(homedir())}]` (주석: "홈 한정 — /Applications 앱 본체는 밖")
   - 사용자가 로컬 파일 능력을 켜도 **홈 밖은 구조적으로 못 쓴다**. `C:\services`, `C:\HD_MEM`,
     `D:\ChatGPT2CodexWorkspace`, 외장 SSD가 전부 여기서 막힌다.
2. **작업 루트를 UI로 바꿀 수 없다.**
   - `src/workspace.mjs:10` — `WS_ROOT = process.env.ARGO_ROOT || process.env.CREWBASE_ROOT || join(process.cwd(),'workspaces')`
   - **환경변수 전용**이다. 설정 화면에 경로 변경 항목이 없다(grep 결과 `ARGO_ROOT` 참조는 이 한 줄뿐).

### v0.1.30 #113과의 관계 — 오해 금지

`4a0eb78`(#113, v0.1.30)은 "외부 CLI 샌드박스 거부를 능력 카드로 승격"이다. 커밋 메시지 원문 확인 결과
**안내를 개선한 것이지 접근을 허용한 게 아니다.** 오히려 검수 반영분 HIGH-1이 이렇게 적혀 있다:

> `fs OFF + 홈 밖 경로는 카드를 올리지 않는다 — 켜기 승인 후 같은 거부를 또 겪으면 '켰는데도 차단'
> 신고 문구를 이 기능이 재생산한다. 홈 안 이동 안내로 분기.`

즉 **홈 밖은 "홈 안으로 옮기세요"로 안내**된다. 제보자들이 원하는 건 그 반대다 — 기존 프로젝트 폴더를
그대로 쓰는 것. 이 클러스터는 v0.1.30으로 닫히지 않았다.

### 제보 목록

| id8 | 일시(UTC) | 제보자 | OS | 요지 |
|---|---|---|---|---|
| `80484f63` | 07-26 13:27 | a565***@gmail.com | Win | 파일 쓰기 권한을 켜고 여러 번 재실행해도 런타임 permission profile이 read-only로 유지 |
| `81250bec` | 07-26 14:19 | vori***@gmail.com | Win | **원인 지목** — writable_roots를 `%USERPROFILE%`로 하드코딩해 홈 밖 프로젝트가 계속 차단 |
| `35f99a68` | 07-26 19:15 | robe***@gmail.com | Win | `C:\services\...`가 허용 루트 밖. 심링크/정션은 실경로 검사로 재차단될 것이라 판단해 포기 |
| `26ec826a` | 07-26 19:31 | robe***@gmail.com | Win | "읽기 전용이면 개발 작업에 거의 쓸 수 없다 — 못 쓰게 해놓으신 건지" |
| `8be09aaa` | 07-26 20:25 | colo***@gmail.com | Win | 재로그인해도 `C:\HD_MEM`이 `blocked by policy`. 런타임 허용 루트에 주입 안 됨 |
| `94e80a93` | 07-26 20:26 | colo***@gmail.com | Win | 워크스페이스를 추가할 도구가 없다 |
| `9b961f46` | 07-26 21:12 | wsry***@gmail.com | Win | `D:\ChatGPT2CodexWorkspace` 읽기가 `blocked by policy` + 회사 워크스페이스도 읽기 전용 |
| `1c79949f` | 07-26 12:23 | with***@gmail.com | mac | 전체 작업 폴더를 외장 SSD로 옮기고 싶다(헤르메스는 이미 그렇게 운영 중) |
| `f6a0a0fc` | 07-26 12:52 | vori***@gmail.com | Win | 워크스페이스를 `C:\` 아래 특정 폴더로 지정하고 싶다 |
| `7dd21c67` | 07-26 18:22 | bkj8***@gmail.com | Win | 볼트 기본 위치 변경 필요 — "C드라이브에서 이걸 다 하기는 좀" |
| `6de33ad6` | 07-26 18:10 | bkj8***@gmail.com | Win | 파일 권한을 어떻게 주는지 모르겠다(안내 부재) |

### 개발 세션에 넘기는 판단 재료

- 이 11건 중 **8건이 Windows**다. Windows 사용자는 프로젝트를 `C:\Users\<id>` 밖에 두는 습관이 강하다.
- 요청은 두 갈래로 갈린다. 섞어서 한 번에 풀려 하지 말 것:
  - **(A) 작업 루트 자체를 옮기고 싶다** — 외장 SSD·D드라이브. 4건. `ARGO_ROOT`를 설정 UI로 노출하는 문제.
  - **(B) 기존 외부 프로젝트 폴더를 크루가 읽고 쓰게 하고 싶다** — 7건. `writable_roots`에 사용자 지정
    루트를 더하는 문제. **보안 경계 결정이 선행**한다(앱 본체 보호를 어떻게 유지할지).
- 현재 하드코딩 주석의 취지는 "`/Applications` 앱 본체는 밖"이다. 목표는 앱 자기보호이지 홈 강제가 아니므로,
  **사용자 지정 루트 허용 + 앱 설치 경로 명시 차단**으로 바꾸면 취지를 유지한 채 풀린다(설계 제안, 미검증).

---

## P0-2 — Gemini OAuth 경로가 벤더 측에서 폐기 (2건)

| id8 | 일시(UTC) | 제보자 | 요지 |
|---|---|---|---|
| `92a02406` | 07-26 00:31 | gyuh***@gmail.com | Gemini OAuth 인증했는데 크루 생성 시 미연결로 표시 |
| `38e5281d` | 07-26 19:47 | crow***@gmail.com | **근본원인 규명** — 아래 |

`38e5281d`는 제보라기보다 **완결된 버그 리포트**다. 환경(Windows 10 / Argo v0.1.30 / Gemini CLI 0.52.0 /
Antigravity CLI 1.1.7)·재현 절차·기대·실제·추가 확인까지 갖췄다. 핵심:

- 공식 Gemini CLI 0.52.0에서 Google 로그인 시:
  `This client is no longer supported for Gemini Code Assist for individuals.` → Antigravity로 이전 안내.
- Argo의 "이 컴퓨터 로그인 사용"은 `이 컴퓨터의 CLI가 로그인되지 않았습니다`로 실패.
- 브라우저 OAuth는 `400 invalid_grant / Invalid code verifier`.
- **Antigravity CLI(`agy`)에서는 OAuth 로그인이 정상**이나 Argo가 그 로그인을 인식하지 못한다.

**이게 직전 핸드오버의 "Gemini OAuth 연결 안 됨 — 가설 2개 기각, 정보 대기"에 대한 답이다.**
우리 코드 결함이 아니라 **Google이 개인용 Gemini Code Assist OAuth 클라이언트를 폐기**한 것. 따라서
기존 경로를 디버깅해봐야 안 고쳐진다.

**요청**: Antigravity OAuth(`agy` 로그인) 인식 지원. 최소한 **현재 실패 메시지를 "구글 정책 변경으로
이 로그인 방식이 중단됐다"로 교체**해 사용자가 자기 설정을 의심하며 시간을 태우지 않게 하는 게 급하다
(이건 큰 작업 없이 지금 가능).

**미검증**: Antigravity CLI의 자격 저장 위치·형식을 우리가 직접 확인하지 않았다. 제보자 서술 인용이다.

---

## P1-1 — 크루 이름이 전부 "서윤"으로 생성 (원인 확정)

| id8 | 일시(UTC) | 제보자 | 요지 |
|---|---|---|---|
| `2853a09c` | 07-26 12:01 | test***@gmail.com | 기획자·디자이너·PM을 이름 지정 없이 생성했는데 이름이 모두 "서윤" |

**근본 원인 (코드 확인)** — `src/persona.mjs:46`:

```
name: <한글 이름 2-3자, 사람 이름처럼>
```

이름 생성 프롬프트에 **기존 로스터가 전달되지 않고 유일성 제약도 없다.** LLM은 같은 조건에서 같은
고빈도 이름을 뽑으므로 충돌이 아니라 **필연**이다. 반면 slug는 이미 중복을 회피한다 —
`src/persona.mjs:140` 주석: `동명 크루 중복 영입 시 기존 카드를 덮어쓰지 않는다(-n)`.
**표시 이름만 무방비**다.

**수정 방향**: 영입 프롬프트에 기존 크루 이름 목록을 넘겨 제외시키거나(1차), 생성 후 서버에서
중복이면 재요청/후보 풀에서 대체(2차). slug 쪽 선례를 그대로 따르면 된다.

**영향**: 첫인상 구간이다. "직원 회사를 만든다"는 제품 약속이 3명 모두 동명이인으로 깨진다.
난이도 대비 체감이 커서 P1로 올렸다.

---

## P1-2 — 장시간 작업이 300초에 죽고 ENOENT로 위장 (재현 필요)

| id8 | 일시(UTC) | 제보자 | 요지 |
|---|---|---|---|
| `529dbe72` | 07-26 05:50 | gilw***@gmail.com | 리릭비디오 제작이 **항상 약 300초 후** `ENOENT ... open '.../argo-codex-*/last.txt'`로 실패. **동일 패턴 4회 재현.** 5분 제한 시 임시 세션 폴더가 last.txt를 읽기 전에 정리되는지 확인 요청 |

**코드 확인**: `src/runners.mjs:527` — `externalExec({ ..., timeoutMs = 300_000, ... })`.
제보자가 관측한 "약 300초"와 **기본 타임아웃이 정확히 일치**한다. 같은 함수가 codex를
`--output-last-message <dir>/last.txt`로 실행하고(553행) `finally`에서 `rm(dir, {recursive:true})` 한다.

**즉 실제 원인은 "파일이 사라져서"가 아니라 "5분 제한에 걸려 codex가 last.txt를 쓰기 전에 죽어서"일
가능성이 높다.** 다만 타임아웃이면 `exec`가 먼저 reject돼 `apiError`로 나가야 하는데 사용자에게는
ENOENT가 보였다 — **경로가 코드만으로는 단정되지 않는다.** QA가 재현해 어느 분기로 새는지 확정해야 한다.

**QA 재현 절차(다음 세션에서 실행)**: 임시 `ARGO_ROOT` + 별도 포트로 격리 서버를 띄우고, codex 러너에
6분 이상 걸리는 작업을 시켜 (1) 실제 실패 문구, (2) 300초 경계 여부, (3) `apiError` 경유 여부를 기록한다.
Supabase env는 `unset` 후 실행한다(`src/synccreds.mjs`가 env를 파일보다 우선).

**연결 트랙**: 이건 작업 큐 S2/S3(`docs/long-job-queue-design.md`)와 같은 뿌리다. 사용자는 이미
"5분 넘는 일"을 시키고 있는데 러너 계층이 5분에서 끊는다. **큐를 만들어도 이 300초를 같이 풀지
않으면 소용없다.**

---

## P1-3 — 회의실 대화 유실 (재현 필요)

| id8 | 일시(UTC) | 제보자 | 요지 |
|---|---|---|---|
| `a26db293` | 07-25 21:07 | artr***@gmail.com | 크루들의 대화 내용이 사라지는 경우가 많다. **특히 회의실에서** |

- 제보 시각(07-25 21:07)은 **v0.1.26 발행(07-25 01:40Z) 이후**다. v0.1.26의 #96(손상 복구가 유령 대화
  부활·전 기기 유실을 확대하던 CRITICAL) 수정 **이후에도 발생**했다는 뜻이다.
- 이후 v0.1.29에 "보관 회의 다시 열기"가 들어갔으나 이 신고와 같은 증상인지는 **대조 안 됨**.
- 재현 정보가 부족하다(빈도·타이밍·기기 수 미상). **제보자에게 역질문이 필요**하다:
  기기를 2대 이상 쓰는지 / 사라지는 게 특정 회의인지 전체인지 / 앱 재시작 후 복구되는지.

---

## P2 — 재현 필요 2건

| id8 | 일시(UTC) | 제보자 | 요지 | 코드 대조 |
|---|---|---|---|---|
| `fc105be0` | 07-26 10:19 | pial***@gmail.com | mac. 모델을 자동에서 다른 걸로 바꾼 뒤 **자동으로 되돌릴 수 없다** | `app/c/[ws]/crew/[slug]/page.jsx:1186` — **러너 셀렉트**에는 `<option value="">자동</option>`이 항상 렌더된다. 그걸 고르면 model도 `''`로 리셋된다(1181행). 다만 **모델 셀렉트에는 자동 항목이 없다**(1191~) → 사용자가 모델 드롭다운만 보고 "없다"고 판단했을 가능성. **발견성 문제로 추정, 실기기 확인 필요** |
| `9b5160db` | 07-20 04:31 | doyu***@gmail.com | shift+엔터가 줄바꿈 대신 바로 전송 | 크루 채팅 입력은 `page.jsx:80`에서 `e.key==='Enter' && !e.shiftKey`로 **올바르다**. 즉 현재 코드상 크루 채팅에서는 재현되지 않아야 한다. **회의실 입력 컴포넌트의 핸들러는 이 grep에 안 잡혔다 — 미확인.** 제보 시점(07-20)에 다른 화면이었을 수 있다 |

---

## 이미 해소 — 제보자 재확인만 필요 (8건)

| id8 | 일시(UTC) | 요지 | 해소 근거 |
|---|---|---|---|
| `8ef5875e` | 07-25 23:58 | OpenAI API 연결했는데 `rer or basic authentication in header` 401 | **#108 / v0.1.28**(07-26 09:58Z) — codex CLI 0.144.1이 env `OPENAI_API_KEY`를 안 읽어 격리 홈 `auth.json`으로 통일. **제보가 발행 4시간 전** |
| `67e0e019` | 07-26 09:52 | 같은 401 | 동일. **제보가 발행 6분 전** — 거의 확실히 구버전 |
| `312aa5e5` | 07-25 19:07 | Opus 5 카탈로그 추가 + 크루별 추론 강도 | **둘 다 반영.** `src/runners.mjs:145` `{ id:'claude-opus-5', label:'Opus 5' }`(커밋 `f0b8840`) · 추론 강도 UI `page.jsx:1197`, codex 매핑 `runners.mjs:463~471` |
| `5f41cd64` | 07-25 19:38 | codex config.toml `writable_roots` 백슬래시 이스케이프 누락 | **반영.** `runners.mjs:518`이 `JSON.stringify`로 이스케이프, 주석에 "신고 2026-07-25" 명시 |
| `0277a6ca` | 07-26 06:59 | OAuth 연결인데 비용이 나가는 것처럼 보여 헷갈린다 | **#111 / v0.1.29** — 구독 턴은 턴 수만 표시, apikey만 청구. 추가로 **#118**(`a7cfe26`, main 머지·미발행)이 "현재 자격 기준 단일 진실"로 잔존 금액 표시를 종결 |
| `f6a0a0fc`(2번 문항) | 07-26 12:52 | OAuth면 표시 금액은 추정이고 실제 과금은 아니겠죠? | 동일. **단 이 사용자에겐 1번 문항(워크스페이스 변경)이 P0-1로 남아 있다 — 회신 시 함께** |
| `f08cd5d3` / `51bfdbae` / `8c2c23ec` / `0d195cae` | 07-16~18 | 클로드 미표시 · 로그인 페이지 안 열림 · 러너 ENOENT · CLI 미설치 안내 | v0.1.9~v0.1.16 구간의 러너 연결·원클릭·PATH(`ensureCliPath`)·벤더 CLI 자동 조달로 처리된 계열. **개별 대조는 안 했다 — 재확인 대상** |

> **회신 대기**: `8ef5875e` 제보자는 본문에 회신받을 다른 주소를 남겼다(Supabase에서 조회).
> 401은 v0.1.28에서 고쳐졌으므로 **업데이트 안내 회신이면 충분**하다.

---

## 기능 요청 (13건)

우선순위는 제품 판단 영역이라 정하지 않았다. 반복도와 구현 무게만 표기한다.

| id8 | 일시(UTC) | 요청 | 메모 |
|---|---|---|---|
| `c9d8dea7` | 07-26 07:37 | **이미 로그인된 크롬에 attach(CDP)하는 브라우저 도구** | 요청 중 가장 상세하다. 현재 새 창 방식은 로그인 세션이 안 붙고 자동화 감지로 차단된다는 한계를 스스로 정리했다. `chrome-devtools-mcp` / `@playwright/mcp` CDP 모드 지목 |
| `0484cc80` | 07-26 12:14 | **기존 회사 지식(LLM wiki·vault)을 연결/마이그레이션하는 창구** | 크루에게 물었더니 피드백을 남기라고 안내했다고. 폴더 단위 기억이라는 North Star와 정면으로 맞닿는 요청 |
| `eb320387` | 07-25 22:43 | 상단/하단에 사용량 표시 | OAuth 연결 사용자. 위 청구액 혼란과 같은 뿌리 |
| `4b2f1f3b` | 07-26 01:16 | 크루가 만든 파일을 **사용자가 원하는 위치**에 생성 | P0-1(A)와 같은 뿌리 — 함께 설계할 것 |
| `9b5c258c` | 07-26 03:34 | 설치된 외부 스킬도 클릭하면 작업 지침서·시키는 문구가 보이게 | |
| `5d5b77a8` | 07-25 22:46 | 크루 영입 시 **페르소나 입력 예제** 제공 | 유튜브 사용법 영상처럼. 초보 진입 |
| `fda57640` | 07-26 20:02 | 기본 세팅 필수 매뉴얼이 보이게 | 위와 같은 온보딩 계열 |
| `88a166f5` | 07-26 20:00 | 기존 대화창을 보면서 설정하도록 **사이드 창으로 설정 열기** | |
| `8ff00cca` | 07-26 12:33 | Slack bot 연결 설명 상세화(봇 토큰 스코프 등) | |
| `2ea2b3ff` | 07-27 04:44 | **Mattermost** 연결 | |
| `c4b5124e` | 07-27 04:53 | 화면 **글자 크기 조정** — 너무 작다 | 접근성. 저비용 |
| `8c5d1db4` | 07-26 19:34 | Windows GUI 제어(computer-use / UI 자동화 MCP) | 크루가 "스킬·도구에서 연결하라"고 안내한 내용을 그대로 붙여넣은 것 — **실제 요청인지 크루 답변 전달인지 모호**. 회신 시 확인 |
| `26ec826a`(후반) | 07-26 19:31 | 로컬 프로젝트 경로 직접 추가 + 세션 쓰기 권한 | P0-1과 동일 사안이라 중복 계상하지 않음 |

---

## QA가 관찰한 구조적 신호 (개발 세션이 봐야 할 것)

1. **크루가 피드백 창구로 사용자를 떠민다.** `35f99a68`·`8be09aaa`·`26ec826a`·`0484cc80`은 전부
   **크루의 답변을 사용자가 그대로 복사해 제출**한 것이다. 크루가 "설정 → 피드백에 이렇게 보내세요"라고
   안내한다. 제보 품질은 좋아지지만, 이는 **제품이 못 하는 일을 크루가 대신 사과하고 있다**는 신호다.
   v0.1.29에서 "메뉴로 떠넘기지 마라"를 프롬프트에 넣었는데, 이 패턴은 그 이후에도 계속됐다
   (`8be09aaa` 07-26 20:25 > v0.1.29 발행 13:11Z). **프롬프트 교정으로는 안 닫힌다 — 기능이 없어서다.**

2. **한 사용자가 같은 벽에 반복해서 부딪힌다.** `robe***` 3건, `colo***` 4건, `gilw***` 4건,
   `bkj8***` 2건. 전부 짧은 간격의 연속 제보다. **한 명이 여러 번 쓰는 건 안 풀려서**이지 요청이
   많아서가 아니다. 제보자 수(17명)보다 **막힌 사용자 수**를 세는 게 실태에 가깝다.

3. **Windows 편중.** 유효 39건 중 Windows UA가 다수이고, P0-1 11건 중 8건이 Windows다. 개발·검증
   기기가 맥뿐이라(핸드오버 "인텔 맥·Windows 실기기 미검증" 이월) **가장 많이 막히는 환경이 가장 덜
   검증된다.** 이 비대칭이 이번 백로그의 배경이다.

4. **로그인 없이는 제보할 수 없다.** `feedback.user_id`는 41건 전부 non-null(테스트 1건 제외).
   핸드오버의 "비로그인 모드 피드백 경로 부재"가 데이터로 확인된다 — **연결 단계에서 막힌 사용자는
   애초에 제보를 못 한다.** 위 통계는 "로그인까지는 성공한 사람들"만의 것이다.

---

## 개발 세션 진행 상황 대조 (2026-07-27 05:08 기준)

중복 배분을 막기 위해 진행 중이던 개발 세션 2개의 작업을 확인했다. **결론: 아래 잔여 30건과 겹치지 않는다.**

| 세션 | 작업 | 상태 | 잔여 목록과의 관계 |
|---|---|---|---|
| Argo Dev #12 | **PR #120** `fix(desktop): Windows Hyper-V 예약 포트에서 사이드카 즉사 — bind 판정 + 즉사 폴백` | MERGED 05:08:15Z (`27c909b`) | **겹침 없음.** 이 백로그에 해당 제보는 없다. 다만 "Windows 편중" 구조 신호와 같은 계열 |
| Argo Dev #13 | **PR #121** `fix(runners): 402 판정 임계 분리 — chat 오탐의 일지 무증상 누락 종결` (OpenRouter 러너 #119 후속) | MERGED (`a49c737`) | **겹침 없음.** OpenRouter는 신규 러너 트랙이라 기존 제보와 무관 |

- #120 3R 검수는 GO였으나 **LOW 3건 + 후속 H7(terminal 후 상태줄 문구)이 미해결로 등록**됐다. QA가 릴리스 전 확인할 것.
- 두 PR 모두 **미발행**이다. 이 백로그의 "해소(미발행)" 항목(#118 청구액)과 함께 다음 릴리스에 실린다.

## 다음 QA 세션이 할 일

1. **P1-2 재현** — 격리 서버(임시 `ARGO_ROOT` + 별도 포트, Supabase env `unset`)에서 codex 6분 작업으로
   300초 경계·실패 문구·`apiError` 경유 여부 확정.
2. **P2 2건 실기기 확인** — 모델 자동 되돌리기(mac), shift+엔터(회의실 입력 컴포넌트 위치부터 특정).
3. **P1-3 역질문 초안 작성** — 회의실 대화 유실 재현 정보 수집(기기 수·복구 여부·특정 회의 여부).
4. **해소 8건 회신 초안** — v0.1.28/29/30 업데이트 안내. 발송은 유건 승인 후.
5. **Windows 실기기 확보 협의** — P0-1의 다수가 Windows다. 지금 구조로는 QA가 재현조차 못 한다.

## 이 문서의 한계 (정직 표기)

- 상태 판정은 **코드·커밋·릴리스 메타 대조**에 근거한다. **라이브 재현은 하지 않았다** — "해소(발행)"는
  "수정이 발행됐다"는 뜻이지 "제보자 환경에서 해결됐다"는 확인이 아니다.
- 07-16~18 4건의 해소는 **개별 커밋 대조를 하지 않았다**. 핸드오버 서술 기반 추정이다.
- Antigravity 관련 사실(`agy` 로그인 정상, Gemini CLI 0.52.0 메시지)은 **제보자 서술 인용**이며 우리가
  재현하지 않았다.
- `9b5160db`(shift+엔터)는 **회의실 입력 핸들러를 찾지 못한 상태**로 판정을 보류했다.
