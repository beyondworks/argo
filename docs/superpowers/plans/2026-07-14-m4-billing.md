# M-4 결제(LS) 기계 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** LS 결제가 entitlements를 구동하는 기계 전부 — 웹훅 수신(Edge Function, 서명 검증) → plan 쓰기 → 페이월 UI(업그레이드 버튼). LS 대시보드 실물 값(체크아웃 URL·webhook secret)은 나중에 끼운다(그때까지 테스트 secret으로 E2E).

**Architecture:** LS 웹훅 → Supabase Edge Function `ls-webhook`(HMAC-SHA256 서명 검증, 유일한 entitlements 쓰기 경로 = 서비스 롤) → 기기들이 45초 사이클로 plan 반영(M-2d). 체크아웃은 LS 호스티드 링크(API 키 불필요) + user_id 커스텀 데이터. UI는 SyncCard 확장 — `paywalled` 구조화 필드로 "페이월"과 "고장"을 구분 표시(M-2d 이월 Minor 해소).

**작업 디렉토리: `/Users/yoogeon/lean-projects/_worktrees/argo-m4`** (메인 체크아웃은 랜딩 세션 사용 중 — 금지). 브랜치 `feat/m4-billing`. Supabase 프로젝트 ref `nvqorpdorlbkhpcvwoty` (Seoul — .env.local의 그 프로젝트).

## Global Constraints

- **강제(ARGO_ENFORCE_PLAN)는 이 마일스톤에서도 기본 off 유지** — 켜는 시점은 출시 결정
- 웹훅: 서명 불일치 401(타이밍 안전 비교), subscription_* 외 이벤트 200 무시, user_id 부재 400. entitlements 쓰기는 이 함수가 유일
- plan 매핑: status ∈ {active, on_trial, past_due, cancelled(말일까지 유지)} → pro / 그 외(expired, paused, unpaid…) → free
- 체크아웃 URL env 부재 시 UI는 "준비 중" — 크래시·빈 href 금지
- UI 문자열 i18n ko/en. 시크릿 평문 금지(웹훅 secret은 `supabase secrets`로만)
- 커밋 prefix, 명시 add, sed -i 금지. 검증: npm test/node --check/npm run build

---

### Task 1: Edge Function `ls-webhook` + 배포

**Files:**
- Create: `supabase/functions/ls-webhook/index.ts`

**Interfaces:**
- Produces: `POST https://<ref>.supabase.co/functions/v1/ls-webhook` — LS 서명 페이로드 수신 → entitlements upsert. (Task 3 E2E와 LS 대시보드 웹훅 등록이 사용)

- [ ] **Step 1: 함수 작성** — `supabase/functions/ls-webhook/index.ts` (Deno):

```ts
// LS(Lemon Squeezy) 결제 웹훅 — entitlements의 유일한 쓰기 경로(서비스 롤).
// 서명(X-Signature, HMAC-SHA256 hex) 검증 실패는 401 — 위조 페이로드로 plan을 못 바꾼다.
// 매핑: subscription_* 이벤트 status가 active/on_trial/past_due/cancelled(말일까지 이용 유지) → pro,
//       그 외(expired/paused/unpaid…) → free. subscription_* 외 이벤트는 200 무시(LS 재전송 폭주 방지).
import { createClient } from 'npm:@supabase/supabase-js@2';

const enc = new TextEncoder();

async function hmacHex(secret: string, body: string): Promise<string> {
  const key = await crypto.subtle.importKey('raw', enc.encode(secret), { name: 'HMAC', hash: 'SHA-256' }, false, ['sign']);
  const mac = await crypto.subtle.sign('HMAC', key, enc.encode(body));
  return [...new Uint8Array(mac)].map((b) => b.toString(16).padStart(2, '0')).join('');
}

// 타이밍 안전 비교 — 길이가 달라도 동일 시간 소모
function safeEqual(a: string, b: string): boolean {
  const ab = enc.encode(a), bb = enc.encode(b);
  let diff = ab.length ^ bb.length;
  for (let i = 0; i < Math.max(ab.length, bb.length, 1); i++) {
    diff |= (ab[i % (ab.length || 1)] ?? 0) ^ (bb[i % (bb.length || 1)] ?? 0);
  }
  return diff === 0;
}

const PRO_STATUS = new Set(['active', 'on_trial', 'past_due', 'cancelled']);

Deno.serve(async (req) => {
  if (req.method !== 'POST') return new Response('method not allowed', { status: 405 });
  const secret = Deno.env.get('LS_WEBHOOK_SECRET');
  if (!secret) return new Response('webhook not configured', { status: 500 });
  const raw = await req.text();
  const sig = req.headers.get('x-signature') ?? '';
  if (!safeEqual(await hmacHex(secret, raw), sig)) return new Response('invalid signature', { status: 401 });

  let evt: { meta?: { event_name?: string; custom_data?: { user_id?: string } }; data?: { attributes?: { status?: string } } };
  try { evt = JSON.parse(raw); } catch { return new Response('bad json', { status: 400 }); }
  const name = String(evt?.meta?.event_name ?? '');
  if (!name.startsWith('subscription_')) return new Response('ignored', { status: 200 });
  const userId = evt?.meta?.custom_data?.user_id;
  if (!userId) return new Response('missing user_id', { status: 400 });
  const status = String(evt?.data?.attributes?.status ?? '');
  const plan = PRO_STATUS.has(status) ? 'pro' : 'free';

  const sb = createClient(
    Deno.env.get('SUPABASE_URL')!,
    Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')!,
    { auth: { persistSession: false } },
  );
  // FK(auth.users)가 쓰레기 user_id를 거른다 — 실패는 500으로 드러내 LS가 재시도하게 둔다
  const { error } = await sb.from('entitlements').upsert({ user_id: userId, plan, updated_at: new Date().toISOString() });
  if (error) return new Response('db error', { status: 500 });
  return new Response(JSON.stringify({ ok: true, plan }), { headers: { 'content-type': 'application/json' } });
});
```

- [ ] **Step 2: 배포 + 테스트 secret**

```bash
# 테스트 secret (LS 실물 도착 시 같은 명령으로 교체) — 값은 셸 변수로만, echo 금지
TEST_SECRET=$(openssl rand -hex 32)
supabase secrets set LS_WEBHOOK_SECRET="$TEST_SECRET" --project-ref nvqorpdorlbkhpcvwoty
# E2E용으로 로컬 파일에만 보관 (0600, 커밋 금지 경로)
printf '%s' "$TEST_SECRET" > .superpowers/sdd/.ls-test-secret && chmod 600 .superpowers/sdd/.ls-test-secret
supabase functions deploy ls-webhook --project-ref nvqorpdorlbkhpcvwoty --no-verify-jwt
```
Expected: secrets set 성공 + deploy 성공(함수 URL 출력). `--no-verify-jwt` 필수 — LS는 JWT를 안 보낸다(보안은 서명이 담당).

- [ ] **Step 3: 스모크** — 서명 없는 POST → 401, GET → 405:

```bash
curl -s -o /dev/null -w '%{http_code}\n' -X POST "https://nvqorpdorlbkhpcvwoty.supabase.co/functions/v1/ls-webhook" -d '{}'   # 기대 401
curl -s -o /dev/null -w '%{http_code}\n' "https://nvqorpdorlbkhpcvwoty.supabase.co/functions/v1/ls-webhook"                  # 기대 405
```

- [ ] **Step 4: 커밋**

```bash
git add supabase/functions/ls-webhook/index.ts
git commit -m "feat(m4): LS 웹훅 Edge Function — 서명 검증, entitlements 유일 쓰기 경로"
```

---

### Task 2: 페이월/플랜 UI — SyncCard 확장 + 체크아웃 버튼

**Files:**
- Modify: `src/sync.mjs` (entitlement 게이트에 `status.paywalled` 구조화 — 1~2줄), `src/entitlement.mjs` 불변
- Modify: `app/c/[ws]/settings/page.jsx` (SyncCard — 818행 부근), `app/i18n.jsx`

**Interfaces:**
- Consumes: `sync.plan`(M-2d가 syncStatus에 노출), `/api/me` → {user:{id,email}}, env `NEXT_PUBLIC_LS_CHECKOUT_MONTHLY`/`NEXT_PUBLIC_LS_CHECKOUT_YEARLY`(부재 허용)
- Produces: `sync.paywalled` boolean (syncStatus)

- [ ] **Step 1: sync.mjs 구조화** — entitlement 게이트 블록(`if (!loadSyncCreds()) { ... }`)에서:
  - 차단 시: `status.paywalled = true;` 를 `status.lastError = ...` 옆에 추가
  - 통과 시(게이트 블록 안, ok일 때): `status.paywalled = false;`
  - 서비스 모드(게이트 밖)는 건드리지 않음 — status 초기화 객체에 `paywalled: false` 기본값 추가
  - `syncStatus()` 반환에 이미 spread로 포함되는지 확인(status 객체 spread면 자동)

- [ ] **Step 2: i18n 키** (ko/en — 원화 표기는 fmtMoney 규칙이 아니라 결제가 USD 단일 글로벌 가격이므로 "$12" 그대로, 단 한국어 문구는 한글):

```js
'billing.plan.free': ['Free', 'Free'],
'billing.plan.pro': ['Pro', 'Pro'],
'billing.paywall': ['멀티기기 동기화는 Pro 플랜입니다 — 지금 회사는 안전하게 보존되고 있고, 업그레이드하면 즉시 이어집니다.', 'Multi-device sync is a Pro feature — your company is safely preserved and resumes the moment you upgrade.'],
'billing.upgradeMonthly': ['Pro 업그레이드 — $12/월', 'Upgrade to Pro — $12/mo'],
'billing.upgradeYearly': ['연간 $120 (2개월 무료)', 'Yearly $120 (2 months free)'],
'billing.comingSoon': ['결제 준비 중 — 곧 열립니다', 'Billing opens soon'],
```

- [ ] **Step 3: SyncCard 확장** — 기존 구조(15초 폴, sync.on/leader/lastTs/lastError) 유지하고:
  1. 타이틀 줄의 상태 pill **옆에** plan pill: `sync?.plan === 'pro'` → `<span className="pill ok">Pro</span>`(billing.plan.pro), `'free'` → 기본 pill Free. plan null이면 생략
  2. `sync?.paywalled`가 true면: lastError 빨간 줄 **대신** 페이월 블록 렌더 — `t('billing.paywall')` 안내문 + 업그레이드 버튼 영역 (아래 4)
  3. paywalled가 아니고 lastError 있으면 기존 빨간 줄 그대로 (— "고장"과 "페이월" 구분 완성)
  4. 업그레이드 버튼 영역(별도 소컴포넌트 `UpgradeButtons` 같은 파일 하단): `/api/me`로 user 확보(카드 내 1회 fetch — DevicesCard 관례), env 2개 중 있는 것만 버튼 렌더:
```jsx
const withRef = (base) => `${base}${base.includes('?') ? '&' : '?'}checkout[custom][user_id]=${encodeURIComponent(user.id)}&checkout[email]=${encodeURIComponent(user.email)}`;
// <a className="btn btn-primary sm" href={withRef(MONTHLY)} target="_blank" rel="noreferrer">{t('billing.upgradeMonthly')}</a>
```
     둘 다 없으면 `<p>{t('billing.comingSoon')}</p>`. env 상수는 파일 상단 `const LS_MONTHLY = process.env.NEXT_PUBLIC_LS_CHECKOUT_MONTHLY || ''` 형태(클라이언트 컴포넌트 — NEXT_PUBLIC 인라인)
  5. **업그레이드 버튼은 paywalled일 때 + plan==='free'이고 sync.on일 때(안내 차원)** 노출 — pro면 숨김

- [ ] **Step 4: 검증** — `node --check src/sync.mjs` + `npm test 2>&1 | grep -E "^# (pass|fail)"`(24) + `npm run build 2>&1 | grep -E "✓ Compiled|Failed|Error" | head -3`. 브라우저 확인은 컨트롤러가 T3에서.

- [ ] **Step 5: 커밋**

```bash
git add src/sync.mjs "app/c/[ws]/settings/page.jsx" app/i18n.jsx
git commit -m "feat(m4): 페이월 UI — plan 표시·페이월/고장 구분·LS 체크아웃 버튼"
```

---

### Task 3: E2E + 문서 [컨트롤러]

판정 기준:
1. **위조 거부**: 잘못된 서명 → 401 (entitlements 무변화)
2. **구독 활성**: 테스트 유저 + `subscription_created(active)` 서명 페이로드 → 200 → entitlements = pro (DB 실측)
3. **만료**: `subscription_expired(expired)` → free
4. **본인 가시성**: 해당 유저 세션 JWT로 plan 조회 → pro/free 반영 (M-2d 읽기 경로 연결)
5. **UI**: 세션 기기 1대 — plan pill 렌더 + (env 없이) comingSoon 문구, paywalled 시뮬(ARGO_ENFORCE_PLAN=1 + free)로 페이월 블록 확인
6. **정리**: 테스트 유저 삭제(cascade), 문서 — SPEC에 M-4 기계 완료(취소선 말고 "기계 완료 — LS 실물 연결·강제 전환은 출시 스텝" 표기), LS 대시보드 5분 가이드(docs/billing-ls-setup.md)
7. 최종 리뷰(opus) → 임시 워크트리 병합 → push

## 알려진 한계 (후속/출시 스텝)

- LS 실물(스토어·상품·webhook secret) 미연결 — 유건 대시보드 세팅 후 secret 교체 + 체크아웃 URL env 2개 주입이 출시 스텝
- 강제(ARGO_ENFORCE_PLAN) 기본 off 유지 — 켜는 날 = 데모 종료 결정. 켠 상태의 실서버 장기 E2E도 그때
- LS 이벤트 순서 역전(재전송) 시 마지막 도착이 이김 — LS는 updated_at 기준 재전송 드뭄, 주문 보증 필요 시 후속
