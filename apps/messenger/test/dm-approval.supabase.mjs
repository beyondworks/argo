// Browser-only fixture for DM agent approval (20260918170000): no credentials or production client imported.
// Emulates the server rules the screen depends on: msgr_dm_approver (opener if present, else earliest remaining member) and
// request visibility (requester or approver only). window.__dmApproval.leave(uid) removes a member like msgr_leave_dm.
export const configured = true, customServer = false, SB_URL = 'http://fixture.invalid', SB_ANON = 'fixture';
const uid='user-me', org='org-fixture', now=new Date().toISOString();
const channel=(id,kind,name,created_by=uid)=>({id,org_id:org,kind,name,created_by,archived_at:null,admin_user_ids:[],crew_memory:true,personal_crews:true});
const state=window.__dmFixture=window.__dmApproval={calls:[],failNext:null,tables:{
 msgr_org_members:[{user_id:uid,org_id:org,role:'owner',display_name:'Fixture Owner',removed_at:null,msgr_orgs:{id:org,name:'Fixture Organization',slug:'fixture',owner_user_id:uid}},{user_id:'user-other',org_id:org,role:'member',display_name:'Fixture Colleague',removed_at:null},{user_id:'user-opener',org_id:org,role:'member',display_name:'Fixture Opener',removed_at:null}],
 msgr_channels:[channel('general','public','Fixture General'),channel('grp','dm','dm:Fixture Group','user-opener')],
 msgr_crews:[['mine','user-me','My Agent'],['other','user-other','Colleague Agent']].map(([id,owner,name])=>({id:`crew-${id}`,org_id:org,owner_user_id:owner,slug:`fixture-${id}`,display_name:name,hosting:'local',status:'active',last_seen_at:now,created_at:now,allow:'all'})),
 msgr_channel_members:[['user-opener','2026-09-18T01:00:00Z'],[uid,'2026-09-18T01:01:00Z'],['user-other','2026-09-18T01:02:00Z']].map(([m,at])=>({channel_id:'grp',member_kind:'user',member_id:m,added_at:at})),
 msgr_channel_crew_requests:[{id:'req-1',channel_id:'grp',crew_id:'crew-other',requested_by:'user-other',status:'pending',created_at:now}],
 msgr_messages:[{id:101,org_id:org,channel_id:'grp',author_kind:'user',author_user_id:uid,kind:'text',body:'Existing conversation must survive favorite changes.',created_at:now,deleted_at:null,mentions:[]}],
 msgr_target_prefs:[],msgr_channel_prefs:[],
}};
const approver=(ch)=>{const c=state.tables.msgr_channels.find(x=>x.id===ch);if(!c||c.kind!=='dm')return null;const us=state.tables.msgr_channel_members.filter(m=>m.channel_id===ch&&m.member_kind==='user');if(us.some(m=>m.member_id===c.created_by))return c.created_by;return [...us].sort((a,b)=>String(a.added_at).localeCompare(String(b.added_at))||String(a.member_id).localeCompare(String(b.member_id)))[0]?.member_id??null;};
state.leave=(who)=>{state.tables.msgr_channel_members=state.tables.msgr_channel_members.filter(m=>!(m.member_kind==='user'&&m.member_id===who));};
function result(call, action){if(state.holdNext===`${call.table||call.rpc}:${call.op||'rpc'}`){state.holdNext=null;return new Promise(resolve=>{state.release=()=>{state.release=null;resolve(result(call,action));};});}state.calls.push(structuredClone(call));if(state.failNext===`${call.table||call.rpc}:${call.op||'rpc'}`){state.failNext=null;return {data:null,error:{message:'Fixture temporary failure. Try again.'}};}return {data:action(),error:null};}
function query(table){let op='select',values,cols='*',one=false;const filters=[];const api={
 select(c='*'){cols=c;return api;},eq(k,v){filters.push(r=>r[k]===v);return api;},neq(k,v){filters.push(r=>r[k]!==v);return api;},is(k,v){filters.push(r=>(r[k]??null)===v);return api;},in(k,vs){filters.push(r=>vs.includes(r[k]));return api;},gt(k,v){filters.push(r=>r[k]>v);return api;},lt(k,v){filters.push(r=>r[k]<v);return api;},
 order(k,o={}){filters.push(Object.assign(()=>true,{__order:[k,o.ascending!==false]}));return api;},limit(n){filters.push(Object.assign(()=>true,{__limit:n}));return api;},contains(){return api;},or(){return api;},ilike(){return api;},like(k,pat){const re=new RegExp('^'+pat.replace(/[.*+?^${}()|[\]\\]/g,'\\$&').replace(/%/g,'.*')+'$');filters.push(r=>re.test(String(r[k]??'')));return api;},not(k,op,pat){if(op==='like'){const re=new RegExp('^'+pat.replace(/[.*+?^${}()|[\]\\]/g,'\\$&').replace(/%/g,'.*')+'$');filters.push(r=>!re.test(String(r[k]??'')));}return api;},maybeSingle(){one=true;return api;},single(){one=true;return api;},
 upsert(v){op='upsert';values=v;return api;},update(v){op='update';values=v;return api;},delete(){op='delete';return api;},insert(v){op='insert';values=v;return api;},
 then(resolve,reject){return Promise.resolve(result({table,op,values},()=>{const all=state.tables[table]??=[];let rows=all.filter(r=>filters.every(f=>f(r)));if(table==='msgr_channel_crew_requests'&&op==='select')rows=rows.filter(r=>r.requested_by===uid||approver(r.channel_id)===uid);const ord=filters.find(f=>f.__order)?.__order;if(ord&&op==='select'){rows=[...rows].sort((a,b)=>{const x=a[ord[0]],y=b[ord[0]];const c=typeof x==='number'&&typeof y==='number'?x-y:String(x??'').localeCompare(String(y??''));return c*(ord[1]?1:-1);});}const lim=filters.find(f=>f.__limit)?.__limit;if(lim!=null&&op==='select')rows=rows.slice(0,lim);
 if(op==='upsert'||op==='insert'){rows=(Array.isArray(values)?values:[values]).map(v=>{const keys=table==='msgr_target_prefs'?['user_id','org_id','target_kind','target_id']:['user_id','channel_id'];const old=op==='upsert'?all.find(r=>keys.every(k=>r[k]===v[k])):null;if(old){Object.assign(old,v);return old;}const row={...v};all.push(row);return row;});}
 else if(op==='update')rows.forEach(r=>Object.assign(r,values));else if(op==='delete')state.tables[table]=all.filter(r=>!rows.includes(r));
 if(table==='msgr_channel_members'&&cols.includes('msgr_channels'))rows=rows.map(r=>({...r,msgr_channels:state.tables.msgr_channels.find(c=>c.id===r.channel_id)}));
 return structuredClone(one?rows[0]??null:rows);
 })).then(resolve,reject);},};return api;}
export const supabase={from:query,auth:{getSession:async()=>({data:{session:{user:{id:uid,email:'fixture@example.invalid'},access_token:'fixture'}}}),onAuthStateChange:()=>({data:{subscription:{unsubscribe(){}}}})},
 rpc:async(name,args)=>result({rpc:name,args},()=>{if(name==='msgr_create_channel'){const id=`created-dm-${state.tables.msgr_channels.length}`;state.tables.msgr_channels.push(channel(id,args.kind,args.name));state.tables.msgr_channel_members.push({channel_id:id,member_kind:'user',member_id:uid},...args.others.map(m=>({channel_id:id,member_kind:m.kind,member_id:m.id})));return id;}if(name==='msgr_dm_approver')return approver(args.ch);if(name==='msgr_leave_dm'){state.tables.msgr_channels=state.tables.msgr_channels.filter(c=>c.id!==args.ch);return true;}return [];}),
 realtime:{setAuth:async()=>{}},channel:()=>{const c={on:()=>c,subscribe:()=>c,send:async()=>{},unsubscribe:async()=>{}};return c;},removeChannel:async()=>'ok',removeAllChannels:async()=>{},storage:{from:()=>({remove:async()=>({data:[],error:null})})},
};
export async function q(p){const {data,error}=await p;if(error)throw new Error(error.message);return data;}
